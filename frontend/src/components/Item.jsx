/*import React, { Component } from 'react'
function Item({desc, amt, id, handleDelete})
{
    return (
        <div>
            <p>{desc}</p>
            <p>{amt}</p>
            <button onClick={handleDelete(id)}>delete</button>
        </div>
    )
}
export default Item;*/